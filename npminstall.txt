# 라우터 요청
https://luckyyowu.tistory.com/346

# express (server)
npm i express

# nodemone (server) : 파일을 변경할 때 마다 바로바로 업데이트
npm i nodemon --save

# concurrently (server): react 서버와 node 서버를 동시에 실행시키기 위한 모듈 
npm i concurrently --save

# axios : 통신수단 (client)
npm i axios

# mysql (sever)
npm i mysql

# react-router-dom (client)
npm i react-router-dom

# cors (sever) : cors 문제 해결
npm i cors

